import { request } from './request'
import { setValidToken, getValidToken, clearValidToken } from '@/utils/handleToken'

export {
    setValidToken,
    getValidToken,
    clearValidToken,
    request
}